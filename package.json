{
  "name": "my-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:cf": "astro build && wrangler pages deploy dist --project-name saint"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.2.8",
    "astro": "^7.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.107.1"
  }
}
